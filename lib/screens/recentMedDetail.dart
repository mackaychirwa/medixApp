import 'package:flutter/material.dart';

class RecentMedicalDetail extends StatefulWidget {
  const RecentMedicalDetail({Key? key}) : super(key: key);

  @override
  State<RecentMedicalDetail> createState() => _RecentMedicalDetailState();
}

class _RecentMedicalDetailState extends State<RecentMedicalDetail> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Card(
          child: Column(
            children: [
              c
            ],
          ),
        ),
      ),
    );
  }
}
